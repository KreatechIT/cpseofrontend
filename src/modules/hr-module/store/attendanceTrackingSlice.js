import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const attendanceTrackingSlice = createSlice({
  name: "attendanceTracking",
  initialState,

  reducers: {},
});

// export const {} = attendanceTrackingSlice.actions;
export default attendanceTrackingSlice.reducer;
